name: lenguas
description: Saving indigenous languages
publish_to: 'none' # Do not change this line ever

version: 1.0.3+4005

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  #  cupertino_icons: ^1.0.3
  #  http: ^0.13.3
  #  provider: ^4.3.3
  #  shared_preferences: ^2.0.7
  #  share:
  #  flutter_inappwebview: ^5.3.2 # TODO: migrate to latest version
  ##  liquid_swipe: ^2.1.0
  #  cached_network_image: ^3.1.0
  #  uuid: ^3.0.4
  #  google_fonts: ^2.1.0
  #  path_provider: ^2.0.2
  #  auto_size_text: ^2.1.0
  ##  settings_ui: ^1.0.0  TODO update or remove for good
  ##  system_settings: ^2.0.0 TODO update for good settings
  #  flutter_json_widget: ^1.0.1
  #  google_api_availability: ^3.0.1
  #  ionicons: ^0.1.1
  #  package_info: ^2.0.2
  ##  video_player: ^2.1.14

  #  # Firebase plugins
  firebase_core: ^1.5.0
  #  firebase_analytics: ^8.3.0
  firebase_auth: ^3.0.2
  #  cloud_firestore: ^2.5.0
  url_launcher: ^6.0.9
  sentry_flutter: ^6.2.0
  shared_preferences: ^2.0.11

  # Authentication
  flutter_facebook_auth: ^3.5.1
  flutter_signin_button: ^2.0.0
  google_sign_in: ^5.0.7
  # -----------------------------------------
  # UI
  pagination_view: ^2.0.0
  ant_icons: ^2.0.0-nullsafety.0
  cached_network_image: ^3.0.0
  fluttertoast: ^8.0.7
  intl: ^0.17.0

  # Network
  graphql_flutter: ^5.0.0
  internet_connection_checker: ^0.0.1+1

  # Dart data class
  json_serializable: ^6.1.1
  freezed_annotation: ^1.1.0
  functional_enum_annotation: ^1.2.1

  # Dependency Injection
  get_it: ^7.1.3
  injectable: ^1.0.4

  # State management
  flutter_bloc: ^7.0.1

  # Caching
  hive: ^2.0.4

  # Misc
  dartz: ^0.10.0-nullsafety.2
  rxdart: ^0.26.0
  path_provider: ^2.0.2
  package_info_plus: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Code generators
  freezed: ^1.1.0
  functional_enum: ^1.1.3
  injectable_generator: ^1.4.1
  build_runner: ^2.0.4
  flutter_native_splash: ^1.3.2

  # Linting
  effective_dart: ^1.3.1

  # Testing
  bloc_test: ^8.0.2
  mocktail: ^0.2.0
  lint: ^1.0.0

flutter_icons:
  android: true
  ios: true
  image_path_android: "img/icon-round-new-trimmed.png"
  image_path_ios: "img/icon-full-new.png"
  adaptive_icon_background: "img/icon-bg-padded.png"
  adaptive_icon_foreground: "img/icon-fg-new-padded.png"

flutter:

  uses-material-design: true

  # Fonts
  fonts:
    - family: FredokaOne
      fonts:
        - asset: assets/fonts/google_fonts/FredokaOne-Regular.ttf

  # Assets
  assets:
    - assets/
    - assets/img/
flutter_native_splash:

  # This package generates native code to customize Flutter's default white native splash screen
  # with background color and splash image.
  # Customize the parameters below, and run the following command in the terminal:
  # flutter pub run flutter_native_splash:create
  # To restore Flutter's default white splash screen, run the following command in the terminal:
  # flutter pub run flutter_native_splash:remove

  # color or background_image is the only required parameter.  Use color to set the background
  # of your splash screen to a solid color.  Use background_image to set the background of your
  # splash screen to a png image.  This is useful for gradients. The image will be stretch to the
  # size of the app. Only one parameter can be used, color and background_image cannot both be set.
  color: "#05001E"
  #background_image: "assets/background.png"

  # Optional parameters are listed below.  To enable a parameter, uncomment the line by removing
  # the leading # character.

  # The image parameter allows you to specify an image used in the splash screen.  It must be a
  # png file and should be sized for 4x pixel density.
  image: ./assets/img/logo_rosa_reduced.png

  # The color_dark, background_image_dark, and image_dark are parameters that set the background
  # and image when the device is in dark mode. If they are not specified, the app will use the
  # parameters from above. If the image_dark parameter is specified, color_dark or
  # background_image_dark must be specified.  color_dark and background_image_dark cannot both be
  # set.
  #color_dark: "#042a49"
  #background_image_dark: "assets/dark-background.png"
  #image_dark: assets/splash-invert.png

  # The android, ios and web parameters can be used to disable generating a splash screen on a given
  # platform.
  #android: false
  #ios: false
  #web: false

  # The position of the splash image can be set with android_gravity, ios_content_mode, and
  # web_image_mode parameters.  All default to center.
  #
  # android_gravity can be one of the following Android Gravity (see
  # https://developer.android.com/reference/android/view/Gravity): bottom, center,
  # center_horizontal, center_vertical, clip_horizontal, clip_vertical, end, fill, fill_horizontal,
  # fill_vertical, left, right, start, or top.
  android_gravity: center
  #
  # ios_content_mode can be one of the following iOS UIView.ContentMode (see
  # https://developer.apple.com/documentation/uikit/uiview/contentmode): scaleToFill,
  # scaleAspectFit, scaleAspectFill, center, top, bottom, left, right, topLeft, topRight,
  # bottomLeft, or bottomRight.
  #ios_content_mode: center
  #
  # web_image_mode can be one of the following modes: center, contain, stretch, and cover.
  #web_image_mode: center

  # To hide the notification bar, use the fullscreen parameter.  Has no affect in web since web
  # has no notification bar.  Defaults to false.
  # NOTE: Unlike Android, iOS will not automatically show the notification bar when the app loads.
  #       To show the notification bar, add the following code to your Flutter app:
  #       WidgetsFlutterBinding.ensureInitialized();
  #       SystemChrome.setEnabledSystemUIOverlays([SystemUiOverlay.bottom, SystemUiOverlay.top]);
  #fullscreen: true

  # If you have changed the name(s) of your info.plist file(s), you can specify the filename(s)
  # with the info_plist_files parameter.  Remove only the # characters in the three lines below,
  # do not remove any spaces:
  #info_plist_files:
  #  - 'ios/Runner/Info-Debug.plist'
  #  - 'ios/Runner/Info-Release.plist'
